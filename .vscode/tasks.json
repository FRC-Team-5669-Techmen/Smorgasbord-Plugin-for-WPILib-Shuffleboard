{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build library",
      "type": "shell",
      "command": ".\\gradlew.bat",
      "args": [
        "build",
        "-Dorg.gradle.java.home=${env:JDK_HOME}"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}